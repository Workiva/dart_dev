language: dart

dart:
  - 2.3.0
  - stable
  - dev

sudo: required
addons:
  chrome: stable

script:
  - pub run dependency_validator -i pedantic
  - pub run dart_dev analyze
  - pub run dart_dev format --assert
  - pub run dart_dev test
